services:
  backend:
    build: ./backend
    container_name: uts-backend
    ports:
      - "5000:5000"     
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - model_data:/app
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: uts-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  model_data:
